import { notFound } from "next/navigation";
import { prisma } from "@/lib/prisma";
export default async function EditMedia({params}:{params:Promise<{id:string}>}){const {id}=await params;const m=await prisma.media.findUnique({where:{id}});if(!m)notFound();return <div><h1 className="text-4xl font-black">{m.title} bearbeiten</h1><form action={`/api/admin/media/${m.id}`} method="post" className="mt-8 grid gap-3 max-w-2xl"><input name="title" defaultValue={m.title} className="rounded bg-white/10 px-4 py-3"/><textarea name="description" defaultValue={m.description} className="rounded bg-white/10 px-4 py-3"/><input name="thumbnailUrl" defaultValue={m.thumbnailUrl} className="rounded bg-white/10 px-4 py-3"/><input name="bannerUrl" defaultValue={m.bannerUrl} className="rounded bg-white/10 px-4 py-3"/><button className="rounded bg-red-600 px-5 py-3 font-bold">Aktualisieren</button></form></div>}
